<Window x:Class="KannadaNudiEditor.Views.Sort.SortWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Sort}" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sort By Section -->
        <TextBlock Text="{DynamicResource SortBy}" FontWeight="Bold" Grid.Row="0"/>
        <Separator Grid.Row="0" Margin="0,20,0,0"/>

        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" Margin="0,0,10,0">
                <ComboBoxItem Content="{DynamicResource Paragraphs}" />
                <ComboBoxItem Content="{DynamicResource Headings}" />
                <ComboBoxItem Content="{DynamicResource Field1}" />
            </ComboBox>

            <TextBlock Text="{DynamicResource Type}" VerticalAlignment="Center" Grid.Column="1" Margin="10,0"/>
            <ComboBox Grid.Column="2" Width="100" Margin="5,0">
                <ComboBoxItem Content="{DynamicResource Text}" />
                <ComboBoxItem Content="{DynamicResource Number}" />
                <ComboBoxItem Content="{DynamicResource Date}" />
            </ComboBox>

            <StackPanel Orientation="Horizontal" Grid.Column="4">
                <RadioButton Content="{DynamicResource Ascending}" IsChecked="True" Margin="10,0"/>
                <RadioButton Content="{DynamicResource Descending}" Margin="10,0"/>
            </StackPanel>
        </Grid>

        <!-- Then By Section 1 -->
        <TextBlock Text="{DynamicResource ThenBy}" FontWeight="Bold" Grid.Row="2" Margin="0,15,0,0"/>
        <Grid Grid.Row="2" Margin="0,35,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" Margin="0,0,10,0"/>
            <TextBlock Text="{DynamicResource Type}" VerticalAlignment="Center" Grid.Column="1" Margin="10,0"/>
            <ComboBox Grid.Column="2" Width="100" Margin="5,0"/>
            <StackPanel Orientation="Horizontal" Grid.Column="4">
                <RadioButton Content="{DynamicResource Ascending}" IsChecked="True" Margin="10,0"/>
                <RadioButton Content="{DynamicResource Descending}" Margin="10,0"/>
            </StackPanel>
        </Grid>

        <!-- My List Has -->
        <TextBlock Text="{DynamicResource MyListHas}" FontWeight="Bold" Grid.Row="3" Margin="0,20,0,0"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,45,0,0">
            <RadioButton Content="{DynamicResource HeaderRow}" IsChecked="True" GroupName="Header"/>
            <RadioButton Content="{DynamicResource NoHeaderRow}" GroupName="Header" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="{DynamicResource OK}" Width="80" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Cancel}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
